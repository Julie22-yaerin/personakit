# Required for Layer 1/2 feature extraction (apps/web API routes, packages/llm-extraction)
ANTHROPIC_API_KEY=

# Optional: overrides the default extraction model (claude-sonnet-5)
PERSONAKIT_EXTRACTION_MODEL=
