{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FounderGTMState",
  "description": "Trạng thái dự án mà AI Cố vấn GTM đọc/ghi. Nạp state này trước mọi session.",
  "type": "object",
  "required": ["product", "model", "stage"],
  "properties": {
    "product": {
      "type": "string",
      "description": "Mô tả sản phẩm 1-2 câu: giải quyết vấn đề gì cho ai"
    },
    "model": {
      "type": "string",
      "enum": ["b2b", "b2c", "both"],
      "description": "Mô hình thị trường"
    },
    "stage": {
      "type": "string",
      "enum": ["prepmf", "mvp", "scaling"],
      "description": "prepmf = chỉ có ý tưởng; mvp = có MVP + khách đầu tiên; scaling = đang tăng trưởng"
    },
    "founder_persona": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "background": { "type": "string" },
        "vulnerability_angle": { "type": "string", "description": "Điểm dễ tổn thương/câu chuyện cá nhân dùng cho nội dung 80%" },
        "common_enemy": { "type": "string", "description": "Kẻ thù chung mang tính triết lý (hiện trạng ngành lỗi thời)" }
      }
    },
    "platforms": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Nền tảng đăng: linkedin, x, email, tiktok, reels, shorts, discord..."
    },
    "owned_assets": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Tài sản sở hữu: email list, Discord kín, Zalo group..."
    },
    "lead_magnets": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Lead magnet hiện có: template, prompt library, blueprint..."
    },
    "calendar_history": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Chủ đề đã đăng để tránh trùng lặp khi sinh lịch mới"
    }
  }
}
